/**
 * To use this, rename to `.env.production` and set the production values here.
 * Production builds must set SNAP_ORIGIN to the audited "npm:" snap ID and
 * SNAP_VERSION to the exact audited release version (no range), so installs
 * are pinned to the audited artifact.
 */
SNAP_ORIGIN=
SNAP_VERSION=
